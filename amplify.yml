version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g npm@10
        - npm --version
        - npm install --legacy-peer-deps
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .npm/**/*
