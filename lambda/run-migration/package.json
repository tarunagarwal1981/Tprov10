{
  "name": "run-migration",
  "version": "1.0.0",
  "description": "Lambda function to run database migrations",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r migration-lambda.zip . -x '*.ts' 'node_modules/@types/*' '*.map'"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^24.6.2",
    "@types/pg": "^8.11.10",
    "typescript": "^5.0.0"
  }
}

